---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: alma8-base
    image: almalinux/8-base
    pre_build_image: true

  - name: ubuntu101
    image: ubuntu:latest
    pre_build_image: true

provisioner:
  name: ansible
verifier:
  name: ansible
